/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}", // Inclua todos os arquivos da pasta src
    ],
    theme: {
      extend: {}, // Personalize sua configuração aqui
    },
    plugins: [], // Adicione plugins do Tailwind aqui, se necessário
  };
  